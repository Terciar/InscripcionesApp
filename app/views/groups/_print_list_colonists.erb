<div>
  <div>
  <h1>Actividades Buena Onda</h1>
	<h1>Lista de Colonos</h1>
	<h2>Grupo: <%= @registrations.first.group.nombre if @registrations.first %></h2>
  <h3> Temporada <%= Time.now.year %></h3>  
 </div>

<table id="registrations-list" class="table table-striped table-hover table-condensed">
  <thead>
    <tr>
      <th>Apellido y Nombre</th>
      <th>Edad</th>
      <th>F. de Nac.</th>
      <th>Domicilio</th>
      <th>Teléfono/s</th>
      <th colspan="4">Ob.y/o Inf.</th>
    </tr>
  </thead>

  <tbody>
    <% @registrations.each do |registration| %>
      <tr>
        <td><%= registration.colonist_name %></td>
        <td><%= registration.colonist_age %></td>
        <td><%= registration.colonist_birtday %></td>
        <td><%= registration.colonist_address %></td>
        <td><%= registration.colonist_telephone %></td>
        <td></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>