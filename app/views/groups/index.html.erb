<div class="container-fluid">
  <div class="form-group">
  <p id="notice"><%= notice %></p>
  <h1>Grupos</h1>
    <%= link_to ' Nuevo grupo', new_group_path, class: "btn btn-primary fa fa-group pull-right" if current_user.is_admin? %>
  </div>

<table id="groups-list" class="table table-striped table-hover table-condensed">
  <thead>
    <tr>
      <th headers="Nombre">Nombre</th>
      <th headers="Sexo">Sexo</th>
      <th headers="Quota">Cupo</th>
      <th headers="Extended quota">Cupo extendido</th>
      <th headers="Initial age">Edad Minima</th>
      <th headers="Maximun age">Edad Maxima</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= group.nombre %></td>
        <td><%= group.sexo %></td>
        <td><%= group.quota %></td>
        <td><%= group.extended_quota %></td>
        <td><%= group.initial_age %></td>
        <td><%= group.maximun_age %></td>
        <td><%= link_to ' Mostrar', group, type: "button", class: "btn btn-default fa fa-group" %></td>
        <td><%= link_to ' Editar', edit_group_path(group), class: "btn btn-warning fa fa-pencil", title: 'Editar Grupo', data: { toggle: 'tooltip', placement: 'top', original_title: 'Editar detalles del Grupo' }  %></td>
        <td><%= link_to ' Eliminar', group, class:"btn btn-danger fa fa-trash-o", method: :delete, data: { confirm: "¿Esta seguro que desea borrar este Proyecto? \n" + group.nombre + ""} %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</div>